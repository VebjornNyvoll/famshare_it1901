spring.datasource.url=jdbc:mysql://localhost:3306/
spring.datasource.username=root
spring.datasource.password=mypassword

server.port=8081

spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL5InnoDBDialect

# // TODO: spring.jpa.hibernate.ddl-auto=update (This was in the original example file, but only workds after removing it. Might be worth looking into.
# worked with this line before I altered the pom.xml file.)